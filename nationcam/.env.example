# Admin page password (client-side only — not secure, baked in at build time)
VITE_PAGE_PASSWORD=your_password_here

# JWT token for admin write operations to PostgREST (baked in at build time)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Then create a JWT with role=web_admin signed with PGRST_JWT_SECRET
VITE_ADMIN_JWT=
