# ── Local Development Only ───────────────────────────────────
# In production (Coolify), these are injected as Docker build args.
VITE_LOGTO_ENDPOINT=http://localhost:3301
VITE_LOGTO_APP_ID=your_logto_app_id
VITE_LOGTO_API_RESOURCE=https://api.nationcam.com
